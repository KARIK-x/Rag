/* LOCUS — Dark archival observatory / precision lab design system */
:root{
  --bg:#06080e; --bg-deep:#030406; --surface:#0a0d14; --surface-elev:#0f1118;
  --text-primary:#f2efe9; --text-bright:#fffdf7; --text-muted:#a8a094;
  --accent:#8a247e; --accent-glow:rgba(138,36,126,.55); --accent-soft:rgba(138,36,126,.12);
  --line:rgba(242,239,233,.08); --line-strong:rgba(242,239,233,.18);
  --font-display:'Cormorant Garamond','Georgia',serif; --font-editorial:'Cormorant Infinitum','Georgia',serif;
  --font-ui:'Space Grotesk',system-ui,sans-serif; --font-mono:'SF Mono',ui-monospace,monospace;
}

*,*::before,*::after{margin:0;padding:0;box-sizing:border-box;}
html{scroll-behavior:smooth; -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale;}
body{font-family:var(--font-ui);background:var(--bg-deep);color:var(--text-primary);line-height:1.5;overflow-x:hidden;}

/* Readability zone: high z-index content over dark 3D */
.read-zone{position:relative;z-index:10;background:rgba(6,8,14,.88);backdrop-filter:blur(18px) saturate(110%); -webkit-backdrop-filter:blur(18px) saturate(110%);}

/* Persistent WebGL behind (verified) */
#world-canvas{position:fixed;inset:0;z-index:0;width:100%;height:100%;opacity:.92;pointer-events:auto;}

/* Sections: generous whitespace, calm, expensive */
section{position:relative;min-height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:10vh 8vw;z-index:5;}

/* Header — sticky glass, minimal */
.header{position:sticky;top:0;z-index:50;background:rgba(6,8,14,.92);backdrop-filter:blur(14px); -webkit-backdrop-filter:blur(14px);padding:1rem 7vw;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid rgba(242,239,233,.07);}
.header a{color:var(--text-primary);text-decoration:none;font-family:var(--font-display);font-weight:300;font-size:.85rem;letter-spacing:.06em;}
.header nav{font-family:var(--font-ui);font-size:.68rem;letter-spacing:.13em;text-transform:uppercase;display:flex;gap:2rem;}
.header nav a{color:var(--text-muted);text-decoration:none;transition:color .25s ease;}
.header nav a:hover{color:var(--text-bright);}

/* Hero — dominant, bright, premium */
.hero{flex-direction:column;text-align:center;padding-top:12vh;padding-bottom:14vh;}
.hero .logo-animated{width:clamp(180px,26vw,340px);height:auto;display:block;margin:0 auto 3rem;filter:drop-shadow(0 0 70px rgba(138,36,126,.35));animation:logoFloat 7s ease-in-out infinite alternate;}
@keyframes logoFloat{from{transform:translateY(0) scale(1);}to{transform:translateY(-18px) scale(1.03);}}

h1.hero-title{font-family:var(--font-display);font-weight:300;font-size:clamp(3.2rem,7.5vw,8rem);line-height:.92;letter-spacing:-.065em;color:var(--text-bright);}
h1.hero-title em{font-style:italic;font-family:var(--font-editorial);font-weight:300;color:rgba(242,239,233,.9);}

.hero-sub{font-family:var(--font-editorial);font-style:italic;font-size:clamp(1.05rem,1.6vw,1.25rem);color:rgba(242,239,233,.72);max-width:640px;margin:1.4rem auto 0;line-height:1.65;letter-spacing:.01em;}
.hero-meta{font-family:var(--font-ui);font-size:.65rem;letter-spacing:.22em;text-transform:uppercase;color:rgba(138,36,126,.75);margin-top:2.5rem;}

/* Search bar — large, dominant, premium */
.search-bar{display:flex;align-items:center;gap:1rem;padding:1.2rem 1.5rem;margin-top:4rem;max-width:680px;margin-left:auto;margin-right:auto;background:rgba(242,239,233,.03);border:1.5px solid rgba(242,239,233,.15);border-radius:6px;transition:border-color .3s, box-shadow .3s;}
.search-bar:focus-within{border-color:rgba(138,36,126,.7);box-shadow:0 0 0 4px rgba(138,36,126,.08);}
.search-bar input{flex:1;background:transparent;border:none;font-family:var(--font-display);font-size:clamp(1.15rem,2vw,1.35rem);color:var(--text-bright);letter-spacing:-.01em;outline:none;padding:.2rem 0;}
.search-bar input::placeholder{color:rgba(242,239,233,.45);font-style:italic;font-family:var(--font-editorial);}
.search-bar button{background:rgba(138,36,126,.92);border:1px solid rgba(138,36,126,.95);color:#fff;font-family:var(--font-ui);font-size:.72rem;letter-spacing:.16em;text-transform:uppercase;padding:.95rem 2.2rem;cursor:pointer;border-radius:4px;transition:all .3s ease;font-weight:600;}
.search-bar button:hover{background:#a12f8a;border-color:#b83a95;box-shadow:0 6px 24px rgba(138,36,126,.35);transform:translateY(-1px);}
.search-bar button:active{transform:translateY(0);}

/* Section typography */
.section-label{font-family:var(--font-ui);font-size:.6rem;letter-spacing:.22em;text-transform:uppercase;color:rgba(138,36,126,.85);margin-bottom:1.5rem;}
.section-title{font-family:var(--font-display);font-weight:300;font-size:clamp(2.4rem,4.2vw,4.6rem);line-height:.95;letter-spacing:-.045em;color:var(--text-bright);margin-bottom:1rem;text-align:left;}
.section-body{font-family:var(--font-ui);font-size:1.05rem;color:rgba(242,239,233,.82);max-width:720px;line-height:1.65;text-align:left;letter-spacing:.005em;}

/* Evidence panel */
.evidence-panel{max-width:760px;margin:0 auto;padding:8vh 7vw;}
.evidence-state{background:rgba(10,8,14,.95);border-left:2.5px solid rgba(138,36,126,.65);padding:3.5vh 3vw;transition:opacity .4s ease;}
.evidence-state h3{font-family:var(--font-display);font-weight:300;font-size:1.3rem;color:var(--text-bright);margin-bottom:.7rem;letter-spacing:-.015em;line-height:1.15;}
.evidence-state p{font-family:var(--font-ui);font-size:.95rem;line-height:1.7;color:rgba(242,239,233,.82);margin-bottom:1.2rem;}
.confidence-tag{display:inline-block;border:1px solid rgba(138,36,126,.45);color:rgba(242,239,233,.78);font-family:var(--font-ui);font-size:.65rem;letter-spacing:.12em;text-transform:uppercase;padding:.4rem 1rem;margin-bottom:1.2rem;}
.source-link{display:block;font-family:var(--font-ui);font-size:.85rem;color:rgba(242,239,233,.55);text-decoration:none;border-bottom:1px solid rgba(242,239,233,.12);padding:.7rem 0;transition:color .2s;letter-spacing:.01em;}
.source-link:hover{color:var(--text-bright);border-color:rgba(138,36,126,.5);}

/* Architecture pipeline */
.pipeline-stage{position:relative;display:flex;align-items:center;justify-content:center;padding:2rem 1rem;border-bottom:1px solid rgba(242,239,233,.08);transition:background .3s, border-color .3s;cursor:pointer;}
.pipeline-stage:hover{background:rgba(138,36,126,.06);border-bottom-color:rgba(138,36,126,.35);}
.stage-label{font-family:var(--font-display);font-weight:300;font-size:1.35rem;color:var(--text-bright);letter-spacing:-.02em;line-height:1.15;}
.stage-desc{font-family:var(--font-ui);font-size:.85rem;color:rgba(242,239,233,.65);max-width:480px;margin-left:1.2rem;line-height:1.45;}

/* Cards (About sections) */
.card-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:1.2rem;margin-top:2.5rem;}
.card{background:rgba(10,10,14,.9);border:1px solid rgba(242,239,233,.1);border-radius:8px;padding:1.4rem 1.5rem;transition:border-color .25s, transform .25s;}
.card:hover{border-color:rgba(138,36,126,.5);transform:translateY(-2px);}
.card h3{font-family:var(--font-display);font-weight:300;font-size:1.1rem;color:var(--text-bright);margin-bottom:.5rem;letter-spacing:-.01em;line-height:1.25;}
.card p{font-family:var(--font-ui);font-size:.85rem;color:rgba(242,239,233,.75);line-height:1.6;}

/* Footer */
footer{position:relative;z-index:10;display:flex;justify-content:space-between;align-items:center;padding:5vh 7vw;border-top:1px solid rgba(242,239,233,.08);font-family:var(--font-ui);font-size:.78rem;color:rgba(242,239,233,.45);letter-spacing:.02em;}
footer img{width:44px;height:auto;opacity:.85;filter:brightness(.95);}

/* Responsive */
@media(max-width:900px){section{padding:8vh 5vw;} .search-bar{max-width:92vw;} h1.hero-title{font-size:clamp(2.8rem,12vw,5rem);}.hero{padding-top:10vh;}}
